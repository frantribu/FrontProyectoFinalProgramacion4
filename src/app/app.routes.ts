import { Routes } from '@angular/router';

export const routes: Routes = [
    { 
        //path: "login", loadComponent: () => import("./Pages/User/UserFormComponent").then(c => c.UserFormComponentComponent) 
    }
];
